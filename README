These scripts must be executed on VSky A series node, A1100, and A1970.
1. To run cluster wizard:
      cluster_wizard
2. To show the veriosn:
      cluster_wizard -v

      Note: The node must be full of drives e.g., 16 drives in VR2000, 70 drives in A1970.
	  
	  
If the wizard can not be run, please try the following in the current directory:
      find . -type f -exec \chmod 755 {} \;